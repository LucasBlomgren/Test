#hangman
# Välkomsttext/ruta
# input ord /bibliotek(lista)
# Visa bild med antal streck för antal bokstäver: _ _ _ _ _
# Gissa ord (input)
# While True: 
# Om rätt: Skriva ut bokstav
# Om fel: rita ut streck gubbe 
# antal gissningar: 7
#Om antal gissningar > 7 : GAME OVER (Gubbe ritas ut, gråtonad skärm)
# om rätt ord: Vinner! (dialogruta)
# Vill du spela igen? Y / N 


import pygame

screen = pygame.display.set_mode([800, 600])
run = True


while run:

    for event in pygame.event.get():
        if event.type == pygame.QUIT:
            run = False

    pygame.draw.line(screen, (255,0,0), [400, 300], [300, 300], 20)

   pygame.display.flip()
